import { vuex as Categories } from './categories'
import { vuex as Products } from './products'
import { vuex as Auth } from './auth'

// Shorthand for ‘Categories: Categories’
export default { Categories, Products, Auth }
